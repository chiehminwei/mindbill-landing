# Mirror these in Vercel → Settings → Environment Variables (Production).

# Airtable — lead storage
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=appzhfpUiJMZf88TI
AIRTABLE_TABLE=Leads

# Retell — AI demo call
RETELL_API_KEY=
RETELL_FROM_NUMBER=
RETELL_AGENT_ID=

# Protects GET /api/leads CSV export — pick a long random string
LEADS_ADMIN_TOKEN=
